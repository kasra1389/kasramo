# kasramo
این یک سایت مقایسه است و برای مقایسه افراد تولید شده است
🎯 هدف پروژه
یک سایت مقایسه‌ای برای معلم‌ها یا کارمندها که:

کاملاً فرانت‌اندی است (فقط HTML، CSS، JavaScript).

بدون نیاز به بک‌اند یا دیتابیس خارجی کار می‌کند.

همه داده‌ها در مرورگر کاربر (LocalStorage) ذخیره می‌شوند.

قابلیت تغییر زبان (فارسی/انگلیسی) و تغییر تم (روشن/تاریک) دارد.

🧩 بخش‌های اصلی سایت
خوش‌آمدگویی (Welcome)

معرفی سریع امکانات سایت.

نمایش ویژگی‌ها و راهنمای شروع.

راهنما (Guide)

توضیح نحوه استفاده از بخش‌های مختلف (مدیریت افراد، مقایسه، نظرات).

مدیریت افراد (People)

افزودن، ویرایش و حذف افراد.

فیلدها: نام، نقش (معلم/کارمند)، امتیاز، مهارت‌ها، نکات مثبت/منفی، سال‌های تجربه، توضیحات.

قابلیت جستجو، فیلتر و مرتب‌سازی.

مقایسه (Compare)

انتخاب ۲ تا ۴ نفر برای مقایسه.

نمایش جدول ویژگی‌ها (نقش، بخش، تجربه، امتیاز، مهارت‌ها، مثبت‌ها، منفی‌ها).

هایلایت بهترین/ضعیف‌ترین مقادیر (مثلاً بیشترین تجربه سبز، کمترین قرمز).

جمع‌بندی مثبت/منفی هر فرد.

نظرات (Comments)

ثبت نظر و امتیاز ستاره‌ای برای هر فرد.

نمایش لیست نظرات به همراه تاریخ و میانگین امتیاز.

امکان ویرایش و حذف نظر.

درباره ما (About)

توضیح کوتاه درباره هدف پروژه.

تماس با ما (Contact)

فرم نمایشی (ارسال واقعی ندارد، فقط ظاهری است).

⚙️ امکانات ویژه
تغییر زبان: فارسی ↔ انگلیسی (با دکمه بالا).

تغییر تم: روشن ↔ تاریک (با دکمه بالا).

ذخیره‌سازی محلی: داده‌ها و تنظیمات در LocalStorage ذخیره می‌شوند.

بازنشانی: امکان پاک‌کردن همه داده‌ها و شروع دوباره.

رابط کاربری واکنش‌گرا (Responsive): روی موبایل و دسکتاپ درست نمایش داده می‌شود.

چیپ‌های تعاملی: برای مهارت‌ها و نکات مثبت/منفی (با Enter اضافه می‌شوند، با کلیک حذف می‌شوند).

🖌️ ظاهر و طراحی
طراحی مدرن با کارت‌ها، دکمه‌ها و تب‌ها.

استفاده از CSS Variables برای تم روشن/تاریک.

فونت قابل تغییر (پیشنهاد: Vazirmatn از Google Fonts برای حس نرم و دلنشین).

📦 ساختار فایل‌ها
index.html → فایل اصلی شامل HTML + CSS + JS.

(اختیاری) style.css و script.js اگر بخواهی جدا کنی.

(اختیاری) پوشه fonts/ اگر فونت محلی استفاده کنی.

🚀 نحوه اجرا
کافیست فایل index.html را در مرورگر باز کنی.

یا روی هاست استاتیک (مثل GitHub Pages، Netlify، Vercel) آپلود کنی.

لینک مستقیم اجرا می‌گیری و می‌توانی با دیگران به اشتراک بگذاری.

✅ جمع‌بندی
این پروژه یک سامانه مقایسه‌ای سبک و کامل است که:

بدون بک‌اند کار می‌کند.

همه امکانات مدیریت، مقایسه و ثبت نظر را دارد.

قابل توسعه است (مثلاً می‌توانی بعداً API یا دیتابیس واقعی اضافه کنی).

